
<section class="ftco-section bg-light">
  <div class="container">
    	<div class="row justify-content-center mb-5 pb-2">
          <div class="col-md-8 text-center heading-section ftco-animate">
            <h2 class="mb-4"><span>Recent</span> <a href="/blog">Blog<a></h2>
            <p>Please, spend a few minute to read our most recent <a href="/blog">blog posts<a> below.</p>
          </div>
        </div>
    <div class="row">
      {% set recentPost = collections.posts.slice(0, 3) %}
      {% for post in recentPost %}
            <div class="col-md-6 col-lg-4 ftco-animate">
              <div class="blog-entry">
                <a
                  href="{{ post.url }}"
                  class="block-20 d-flex align-items-end"
                  style="background-image: url('{{ post.data.coverImage }}')"
                >
                  <div class="meta-date text-center p-2">
                    <span class="day">{{ post.date | dateToISO('d') }}</span>
                    <span class="mos">{{ post.date | dateToISO('MMMM') }}</span>
                    <span class="yr">{{ post.date | dateToISO('yyyy') }}</span>
                  </div>
                </a>
                <div class="text bg-white p-4">
                  <h3 class="heading">
                    <a href="{{ post.url }}">{{ post.data.title }}</a>
                  </h3>
                  <p>
                    {{ post.data.description }}
                  </p>
                  <div class="d-flex align-items-center mt-4">
                    <p class="mb-0">
                      <a href="{{ post.url }}" class="btn btn-secondary"
                        >Read More <span class="ion-ios-arrow-round-forward"></span
                      ></a>
                    </p>
                  </div>
                </div>
              </div>
            </div>
      {% endfor %}
    </div>
  </div>
</section>